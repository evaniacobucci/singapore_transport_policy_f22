# Really we just need to look at the distributions for the target concepts
78
79
717
89
817
917

Scraps:
  # Plot distributions of all the pairs
  num_cols <- unlist(lapply(control, is.numeric)) 
num_cols

data_num <- control[ , num_cols]                        # Subset numeric columns of data
data_num                                             # Print subset to RStudio console

for(i in names(data_num)){
  hist(data_num[i],xlab=i,main="")
}